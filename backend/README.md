# Backend – Compliance Autofill Engine

FastAPI backend that integrates with AWS Bedrock (Claude) to:
- Extract compliance-relevant information
- Autofill regulatory language
- Flag missing or risky sections
- Provide human-readable explanations

## Setup
```bash
python -m venv venv
source venv/Scripts/activate
pip install -r requirements.txt
